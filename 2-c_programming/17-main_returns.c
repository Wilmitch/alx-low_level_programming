#include <stdio.h>

/**
 * return tells the shell how the program exited
 * to check the status on the command line we use: echo $?
 * 0 is default for signaling success while any other signals error.
 */

int main(void)
{
	return 0;
}
