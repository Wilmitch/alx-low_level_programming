#include <stdio.h>

/**
 * main - entry point
 * This program prints a string on the screen using the function printf
 * Return: It will always return 0 on success
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
