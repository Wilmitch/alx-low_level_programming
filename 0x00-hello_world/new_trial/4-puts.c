#include <stdio.h>

/**
 * This program prints a string followed by a new line on the std output
 * returns 0 always on success
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return 0;
}
